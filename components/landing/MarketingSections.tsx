"use client";

export default function MarketingSections() {
  return (
    <div className="qa-ref-marketing" id="how-it-works">
      <section className="qa-ref-card qa-ref-marketing__panel scroll-mt-24">
        <p className="qa-ref-kicker">Intelligence doctrine</p>
        <h2 className="qa-ref-h2 mt-3">Price, trust, and timing — aligned before checkout</h2>
        <p className="qa-ref-lead qa-ref-lead--narrow mt-3">
          QuantAI synthesizes live market posture into clear buy, wait, or avoid reads.
        </p>
      </section>
    </div>
  );
}
