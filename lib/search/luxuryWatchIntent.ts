/**
 * Luxury watch intent — backward-compatible re-exports from taste grammar registry.
 * Phase 1 ranking paths import from here; implementation lives in luxuryWatchGrammar.
 */

export {
  hasLuxuryWatchIntent,
  isConsumerFitnessWatchListing,
  isLuxuryWatchListingEvidence,
  luxuryWatchIntent01,
} from "@/lib/taste/grammars/luxuryWatchGrammar";
